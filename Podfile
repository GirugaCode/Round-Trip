# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Round Trip' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Round Trip
  pod 'GoogleMaps'
  pod 'GooglePlaces'

  target 'Round TripTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Round TripUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
